import { Metadata } from "next";
export const metadata: Metadata = { title: "Tracking Paket | Nadebee Express" };
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</>; }