import { Metadata } from "next";
export const metadata: Metadata = { title: "Riwayat Pelanggan | Nadebee Express" };
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</>; }