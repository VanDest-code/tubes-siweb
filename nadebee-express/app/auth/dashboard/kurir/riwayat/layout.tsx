import { Metadata } from "next";
export const metadata: Metadata = { title: "Riwayat Kurir | Nadebee Express" };
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</>; }